# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Node
node_modules

.wrangler
dist

coverage
.claude

# Config backups (contain configuration values)
backups/
proxy-config-backup-*.json
